{% extends 'cabinet/cabinet_layout.html.twig' %}

{% block title %}{{ 'Profile'|trans }}{% endblock %}

{% block content %}
    <div class="col-lg-12">
{#  START FORM  #}
        {{ form_start(profileForm, {'action': path('post_profile_edit'), 'method': 'POST', 'attr': {'id': 'profileForm','class':'mb-3','novalidate':true}}) }}
        {{ form_row(profileForm._token) }}
        <div class="card card-default">
            <div class="card-body text-center">
                <div class="py-4"><img class="img-fluid rounded-circle img-thumbnail thumb96"
                                       src="
                                                        {% if user.avatar %}
                                                            {{ asset(user.avatar) }}
                                                        {% else %}
                                                            {{ asset('img/user/02.jpg') }}
                                                        {% endif %}
                                                       " alt="{{ 'Contact'|trans }}"></div>
{#                <label class="btn btn-primary"><input type="file" name="avatar"></label>#}
                {{ form_widget(profileForm.avatar) }}
                <div class="form-group">
                    <div class="offset-5 col-2">
                        {{ form_row(profileForm.save_avatar, {'label': 'Edit profile'|trans}) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="card card-default">
            <div class="card-header d-flex align-items-center">
                <div class="d-flex justify-content-center col">
                    <div class="h4 m-0 text-center">{{ 'Contact Information'|trans }}</div>
                </div>
            </div>
            <div class="card-body">
                <div class="row py-4 justify-content-center">
                    <div class="col-12 col-sm-10">
                        <div class="form-group">
                            {{ form_label(profileForm.first_name,'First name'|trans, {
                                'label_attr': {'class': 'text-muted'}
                            }) }}
                            <div class="input-group with-focus">
                                {{ form_widget(profileForm.first_name) }}
                                <div class="input-group-append">
                             <span class="input-group-text text-muted bg-transparent border-left-0">
                                 <em class="fa fa-user"></em>
                             </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(profileForm.second_name,'Second name'|trans, {
                                'label_attr': {'class': 'text-muted'}
                            }) }}
                            <div class="input-group with-focus">
                                {{ form_widget(profileForm.second_name) }}
                                <div class="input-group-append">
                             <span class="input-group-text text-muted bg-transparent border-left-0">
                                 <em class="fa fa-user"></em>
                             </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(profileForm.last_name,'Last name'|trans, {
                                'label_attr': {'class': 'text-muted'}
                            }) }}
                            <div class="input-group with-focus">
                                {{ form_widget(profileForm.last_name) }}
                                <div class="input-group-append">
                             <span class="input-group-text text-muted bg-transparent border-left-0">
                                 <em class="fa fa-user"></em>
                             </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(profileForm.first_name,'Phone'|trans, {
                                'label_attr': {'class': 'text-muted'}
                            }) }}
                            <div class="input-group with-focus">
                                {{ form_widget(profileForm.phone) }}
                                <div class="input-group-append">
                             <span class="input-group-text text-muted bg-transparent border-left-0">
                                 <em class="fa fa-phone"></em>
                             </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(profileForm.email,'Email address'|trans, {
                                'label_attr': {'class': 'text-muted'}
                            }) }}
                            <div class="input-group with-focus">
                                {{ form_widget(profileForm.email) }}
                                <div class="input-group-append">
                             <span class="input-group-text text-muted bg-transparent border-left-0">
                                 <em class="fa fa-envelope"></em>
                             </span>
                                </div>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="offset-5 col-2">
                                {{ form_row(profileForm.save, {'label': 'Edit profile'|trans}) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{ form_end(profileForm) }}
{#  FINISH FORM #}
    </div>
{% endblock %}
 {% block javascripts %}{% endblock %}
